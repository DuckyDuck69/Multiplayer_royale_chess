import Move from "./move";

export default class NPC {
    constructor(piece, directionLlist){
        this.piece = piece
        this.directionLlist = directionLlist
    }
    getRandomMove(state){
        
    }
}